spring:
  data:
    mongodb:
        uri: ---

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

telegram.bot.name: ---
telegram.bot.key: ---


